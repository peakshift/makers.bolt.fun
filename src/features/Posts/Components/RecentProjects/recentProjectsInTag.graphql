query recentProjectsInTag($tagId: Int!) {
  recentProjectsInTag(tagId: $tagId) {
    id
    title
    thumbnail_image
    hashtag
    category {
      id
      icon
      title
    }
    votes_count
  }
}
